#include "function.h"

int main (void)
{
	//FILE *infile = NULL;
	int a[100] = {0}, b[101] = {0}, num = 0;
	int counter = 0;

	//infile = fopen ("Text.txt", "r");

	//pop_array (infile, a, &num);
	//rev_array (a, b, num);

	//for (int count = 0; count < num; count++)
	//{
	//	printf ("%d", b);
	//}

	/*pop_array2 (a, 20);
	number (a, b, 20);

	for (;counter<101; counter++)
	{
		printf ("b[%d]: %d\n", counter, b[counter]);
	}*/
	
	return 0;
}